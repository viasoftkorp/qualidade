import { IKeyTranslate } from '@viasoft/common';

export interface GeralI18n extends IKeyTranslate {
  Configuracoes: {
    Gerais: {
      Salvar: string,
      OdfRetrabalho: string,
      ConsiderarApenasSaldoApontado: string,
      AlterarRelatorio: string
    }
  }
}
