import { IKeyTranslate } from '@viasoft/common';

export interface InspecaoEntradaI18n extends IKeyTranslate {
  InspecaoEntrada: {
    Navigation: {
      Title: string;
      Processamento: string;
      Historico: string;
      Configuracoes: string;
      PlanoAmostragem: string;
      AlterarRelatorio: string;
    };
    Permissions: {
    };
  };
}
